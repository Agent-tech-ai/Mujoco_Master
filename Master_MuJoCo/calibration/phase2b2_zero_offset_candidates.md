# Phase 2B-2 zero and encoder-offset candidates

Status: **NO CANDIDATES GENERATED**

No active motion was performed, so there is no command/response evidence from which to propose a zero offset or encoder offset.

The passive snapshot showed small differences between the existing MC command and measured position for the four proposed joints. Those differences can include tracking error, compliance, load, controller behavior, and measurement timing. They are not treated as zero or encoder-offset evidence.

All four hardware zero and encoder-offset fields remain `UNKNOWN` in `joint_mapping.csv`.

